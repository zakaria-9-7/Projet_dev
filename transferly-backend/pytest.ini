# pytest.ini
# Fichier : transferly-backend/pytest.ini
#
# Configure pytest pour qu'il trouve les tests et le module app correctement.

[pytest]
testpaths = tests
pythonpath = .
addopts = -v --tb=short